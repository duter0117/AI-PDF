[build]
command = "pip install -r requirements.txt && pip uninstall -y opencv-python opencv-python-headless && pip install opencv-python-headless"

[start]
command = "python -m uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
